# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'itunes-poc' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'RxSwift', '4.4.2'
  pod 'networkLayer', :path => '/Users/davidduarte/Desarrollos/Propios/networkLayer'
  # Pods for itunes-poc

  target 'itunes-pocTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
